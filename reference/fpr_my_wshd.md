# PUll out watershed characteristics

PUll out watershed characteristics

## Usage

``` r
fpr_my_wshd(site = my_site, col = area_km)
```

## Arguments

- site:

  Integer - defaults to my_site

- col:

  string unquoted representing column to pull from wshds object
