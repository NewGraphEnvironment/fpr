# Summary stats for each watershed

Summary stats for each watershed

## Usage

``` r
fpr_table_wshd_sum(dat = wshds, site_id = NULL)
```

## Arguments

- dat:

  Dataframe with stream_crossing_id, area_km

- site_id:

  Numeric id corresponding to stream_crossing_id (psscis id) of site
