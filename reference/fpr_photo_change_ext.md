# Rename file extensions to what they will be after the change

Rename file extensions to what they will be after the change

## Usage

``` r
fpr_photo_change_ext(filenames_to_change)
```

## Arguments

- filenames_to_change:

  String full path names of photos we want to change

## Value

String of changed file name
