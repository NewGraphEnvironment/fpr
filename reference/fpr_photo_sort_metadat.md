# Get photo sorting specific metadata from the photos in the file

Get photo sorting specific metadata from the photos in the file

## Usage

``` r
fpr_photo_sort_metadat(input_file)
```

## Arguments

- input_file:

  String full path to directory containing photos we need sorted by time

## Value

A tibble
